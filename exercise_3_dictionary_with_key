#3: Viết function đọc các câu trong một file txt, đếm số lượng các từ xuất hiện và trả về một dictionary với key là từ và value là số lần từ đó xuất hiện.

import gdown

# Tải file từ Google Drive
!gdown https://drive.google.com/uc?id=1IBScGdW2xlNsc9v5zSAya548kNgiOrko

def word_count(file_path):
    # Tạo dictionary để lưu số lần xuất hiện của các từ
    word_count_dic = {}
    
    # Đọc file và đếm số lần xuất hiện của từng từ
    with open(file_path, 'r') as file:
        for line in file:
            # Chuyển tất cả các từ thành chữ thường và tách chúng ra
            words = line.lower().split()
            for word in words:
                if word in word_count_dic:
                    word_count_dic[word] += 1
                else:
                    word_count_dic[word] = 1
    
    return word_count_dic

# Đường dẫn đến file đã tải về
file_path = 'P1_data.txt'

# Đếm số lần xuất hiện của các từ trong file
result = word_count(file_path)
print(result)