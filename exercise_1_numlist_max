#1: Cho một list các số nguyên num_list và một sliding window có kích thước size k dichuyển từ trái sang phải. Mỗi lần dịch chuyển 1 vị trí sang phải có thể nhìn thấyđươc k số trong num_list và tìm số lớn nhất trong k số này sau mỗi lần trượt k phải lớn hơn hoặc bằng 1

def max_sliding_window(nums, k):
    
    result = []
    num_test = []

    for i in range(len(nums)):
        # Remove elements not within the sliding window
        if num_test and num_test[0] < i - k + 1:
            num_test.pop(0)
        
        # Remove elements smaller than the current element from the num_test
        while num_test and nums[num_test[-1]] < nums[i]:
            num_test.pop()
        
        num_test.append(i)
        
        # Append the maximum element of the current window to the result list
        if i >= k - 1:
            result.append(nums[num_test[0]])

    return result

# Input list and k value
num_list = [3, 4, 5, 1, -44, 5, 10, 12, 33, 1]
k = 3
